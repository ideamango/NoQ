//To generate json serializable data model part.
flutter pub run build_runner build

//Retrieve value from firebase snapshot

Map<dynamic, dynamic> map = snapshot.data.snapshot.value;

map.values.toList()[index]["pic"]

To give rounded box
// decoration: BoxDecoration(
//                 color: Colors.orange,
//                 shape: BoxShape.rectangle,
//                 borderRadius: BorderRadius.only(
//                     topLeft: Radius.circular(25.0),
//                     bottomRight: Radius.circular(25.0))),

 void _showSnackBar(String pin, BuildContext context) {
    final snackBar = SnackBar(
      duration: Duration(seconds: 3),
      content: Container(
          height: 80.0,
          child: Center(
            child: Text(
              'Pin Submitted. Value: $pin',
              style: TextStyle(fontSize: 25.0),
            ),
          )),
      backgroundColor: Colors.deepPurpleAccent,
    );
    Scaffold.of(context).hideCurrentSnackBar();
    Scaffold.of(context).showSnackBar(snackBar);
  }
}



Container(
              width: double.infinity,
              decoration: BoxDecoration(
                  border: Border.all(color: Colors.indigo),
                  color: Colors.grey[50],
                  shape: BoxShape.rectangle,
                  borderRadius: BorderRadius.all(Radius.circular(5.0))),
              child: Column(
                mainAxisAlignment: MainAxisAlignment.start,
                children: <Widget>[
                  adrsField1,
                  adrsField2,
                  Row(
                    children: <Widget>[
                      localityField,
                      cityField,
                    ],
                  ),
                  Row(
                    children: <Widget>[
                      pinField,
                      countryField,
                    ],
                  ),

                  // if (_isPositionSet == true)
                  //   new Flexible(
                  //       child: ListView.builder(
                  //           itemCount: _addressList.length,
                  //           itemBuilder: (BuildContext cxt, int index) {
                  //             return Container(
                  //               child: new Column(
                  //                   children: _addressList
                  //                       .map(_buildAddress)
                  //                       .toList()),
                  //               //children: <Widget>[firstRow, secondRow],
                  //             );
                  //           })),
                  MaterialButton(
                    minWidth: MediaQuery.of(context).size.width,
                    autofocus: false,
                    onPressed: _getCurrLocation,
                    child: Text('Auto populate using current location'),
                  ),